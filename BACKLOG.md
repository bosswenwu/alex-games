# Alex Games 优化待办（带优先级）

> 用途：额度中断后续接的唯一入口。开工前先读本文件 + 对应游戏目录里的 `PROGRESS.md`（本地开发产物，已在 .gitignore，不入库）。
> 约束（全局）：单文件/零外部依赖优先、纯代码绘制美术、每款都要 `window.__game` 调试接口、每步保持可玩（Loop Engineering）、agent 不碰 git（由主会话统一提交推送）。
> 验证限制：无头浏览器面板不合成帧、rAF 不推进，运动手感需真机/显示态浏览器点测；能断言的（语法、状态、数值）先断言。

最后更新：2026-08-23。已上线：仓库瘦身、首页数据化、坦克步骤1–6+步骤2演出、沙海多群系+图鉴+探索兑现、竹知了方案B（去 vendor 14M）、Haven 拆包+光照+音频、深渊新敌人+多阶段Boss、三国/坦克音频。手感/摄像头/存档仍需线上点测。

---

## 🧭 统筹与协作（2026-08-23 起）

**Claude Code 统筹此仓库,Cursor 协助。** 分工与不撞车协议:

- **车道划分**（认领即独占该文件，改前看对应 `PROGRESS.md` + `git fetch`）：
  - Claude Code（我，统筹）：`index.html` 首页、仓库结构/部署/`vercel.json`、**跨游戏一致性与集成 QA**（每有改动落地就核验线上不坏）、`games/nebula/`、以及我明确认领的单游戏切片。
  - Cursor（协助，深耕单游戏）：当前在 `abyss` / `tank-strike` / `three-kingdoms` / `zhuzhiliao` / `minecraft` 深挖内容与美术。这些游戏由 Cursor 主导，我不并行改同一文件。
- **协议**：① 动手前 `git fetch` + 看目标文件的 `PROGRESS.md`；② 认领就写 `games/<g>/PROGRESS.md`（同磁盘对方可见）；③ 推之前先 `git fetch` + ff 合并，绝不 `--force`；④ 分支→PR→复核后合并，谁都别碰对方 PROGRESS 正认领的文件。
- **我作为统筹的固定职责**：跨游戏集成 QA（多方快速推送到线上，需有人逐个核验加载/无回归）、复核并合并 Cursor 的 PR、路线图排序、把控"克制/零依赖/可验证"的质量红线。

---

## P0 · 快赢 / 收尾（1–2 步就能完成，风险低）

- [x] ~~**竹知了 CDN 决策落地**~~ — 用户已选**方案 B**（2026-08-22）：`git rm` 掉 `vendor/`，保留本地 `three.module.js`。Hands = jsdelivr 主 + unpkg 备 + 8s 超时；启动层中文失败提示 + 重试；摄像头按拒绝/无设备/非 HTTPS 分流。手势可选。`.git` 历史里的 14MB blob 需日后 `git filter-repo` 才真正清除。

- [x] ~~**Haven 首屏拆包**~~ — `games/haven/`：`index.html` 为外链壳（`styles.css` + `haven.js` + `assets/`）。逻辑只改 `haven.js`，**不再双维护** `haven.html`（整包遗留，不作第二份源）。验收仍待真机：旧存档 `haven.world.v1`、建造/世界/UI。

---

## P1 · 头牌玩法（用户核心诉求：上强度）— 已上线，待线上点测/深化

- [x] ~~**坦克"更像坦克" 步骤 1–6**~~ — `games/tank-strike/index.html`：车身/炮塔分离+履带、装甲角度跳弹、3 弹种、掩体、敌人军种+Boss 弱点。已加载核验零报错。**待点测**：运动手感、跳弹/弹种平衡、Boss 弱点可破性。
- [x] ~~**沙海奇境 多群系（首批）**~~ — `games/minecraft/index.html`：11 群系（含火山/盐湖/峡谷/晶洞高地/古代遗迹带）+ 地标（沉船带金币图纸/商队残骸/晶洞）+ 指南针 + 精简帮助（Cursor 完成）。
  - [x] **群系探索图鉴**（Claude Code，已上线）：首次踏入群系 +15XP、集齐 11 个 +100XP，`__game.codex`，存档持久 + 老存档兼容（双向验证通过）。
  - [x] **探索兑现**（Cursor，本波）：抵达沉船/商队/晶洞/遗迹给背包掉落+XP，图纸解锁「沉船灯塔」「晶脉提纯」；按群系产材；干旱带可滚沙暴（能见度骤降，露天掉血，进掩体免疫）。`__game.payoff`。
  - [ ] **仍待**：海市蜃楼、载具深化。空闲图集格：238–249。

---

## P2 · 全站打磨（各游戏共性短板，可穿插做）

- [x] **音频（三国 / 坦克 / Haven）**：三国栈已在 main。坦克暂停混音器 + 变体 + 胜负 Boss 音色；Haven 暂停层注入分轨（`haven.audio.v1`，不改世界存档）。全 WebAudio，不下 mp3。听感待真机点测。
- [ ] **首因体验**：主菜单动态背景+标题演出+音乐淡入；载入过渡不硬切。
- [ ] **可读性/新手引导**：分步引导可跳过；图标状态可悬停查解释。
- [ ] **设置完备**：画质档位(低配关粒子/阴影)、键位说明、完整暂停菜单。
- [ ] **深渊圣所**（本地 Desktop 版更超前，仓库版待同步）：新敌人待机/前摇/死亡逐帧动画（art.js 自承没做到）；铁砧/石台/熔炉美术细节收尾。
- [ ] **首页 P2**：设备需求标签（竹知了=摄像头、深渊=键鼠）；封面统一风格。

---

## 已知环境事实

- 部署：GitHub Pages（主 `bosswenwu.github.io/alex-games/`）+ Vercel 镜像（`alex-games-wheat.vercel.app`），推 `main` 两处都更新。
- 曾有并发 agent 带 git 权限直接提交推送——多方协作时注意先 `git pull`/核对 origin 再推，避免互相覆盖。
- 反复撞「每周额度」硬顶。续接靠各目录 `PROGRESS.md` 断点。

---

## Cursor 队列进度（2026-08-23，追加）

- **竹知了方案 B 已落地**（用户拍板）：`git rm` 掉 `vendor/`，保留本地 `three.module.js`。Hands = jsdelivr 主 + unpkg 备 + 8s 超时；启动层中文失败提示 + 重试；摄像头按拒绝/无设备/非 HTTPS 分流。手势可选。`.git` 历史里的 14MB blob 需日后 `git filter-repo` 才真正清除。
- **Haven 已拆包**：`haven.html` 外链 `haven.js` + `styles.css` + `assets/`，去掉 4 处 base64，不再双维护。待真机点测：旧存档 `haven.world.v1`、建造/世界/UI、console。
- **一致性**：三国菜单「25 波」→「30 波」（第 25 波曹操亲征文案保留，那是真实波次）；坦克「矢量履带」现在会缩短 `dashMax`。
- **性能**：三国 `victory`/`gameover` 后停 rAF，开新局/回菜单再 `kickLoop`；沙海粒子按 `RENDER` 封顶，低配跳过天气粒子。
- **沙海深化**：复用死分支 `BIO_FOREST`=晶洞高地、`BIO_TAIGA`=古代遗迹带（未新开枚举）。地标：晶洞入口 / 沉船 / 商队残骸。新方块走图集 **235–237**（209–234 仍是罗马敌人）。剩余空闲格：**238–249**。
- 负责的五款游戏均已加「返回游戏厅」。坦克步骤 5（平衡点测）留给线上。

## Cursor 进度（2026-08-23 队列 1→4，追加）

- **① 沙海探索兑现**：指南针所指沉船/商队/晶洞/遗迹首次抵达给真实背包掉落+XP；图纸锁「沉船灯塔」「晶脉提纯」。挖掘按群系额外产材。干旱带可滚沙暴（雾距压到约 16、露天掉 1 血，头顶有实心方块则免）。不重做图鉴。`landSeen` 写入 `sandsea_save_v1`，老存档缺字段不崩。
- **② 音频**：三国已在 main。坦克/Haven 混音器+变体随本 PR 带上（WebAudio，不下 mp3）。
- **③ 坦克步骤 2**：已在 main（`recoilV`/`turretRecoil`/`hullPitch`/`muzzle`、装填环、炮口烟），本波核实后未再改手感。
- **④ BACKLOG**：顶部与 P0 不再写方案 A / 20M vendor /「haven.html 保留为离线单文件包」。
