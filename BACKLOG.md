# Alex Games 优化待办（带优先级）

> 用途：额度中断后续接的唯一入口。开工前先读本文件 + 对应游戏目录里的 `PROGRESS.md`（本地开发产物，已在 .gitignore，不入库）。
> 约束（全局）：单文件/零外部依赖优先、纯代码绘制美术、每款都要 `window.__game` 调试接口、每步保持可玩（Loop Engineering）、agent 不碰 git（由主会话统一提交推送）。
> 验证限制：无头浏览器面板不合成帧、rAF 不推进，运动手感需真机/显示态浏览器点测；能断言的（语法、状态、数值）先断言。

最后更新：2026-08 第三轮之后。已上线：仓库瘦身 13M→6.6M、首页数据化、坦克步骤1。

---

## P0 · 快赢 / 收尾（1–2 步就能完成，风险低）

- [ ] **竹知了 CDN 决策落地** — `games/zhuzhiliao/`
  断点：MediaPipe(~25MB) 与 three.js(1.27MB) 已下到 `vendor/`（.gitignore 中），但 three 的 importmap 仍指 unpkg CDN，本地化只做了一半，已回退到线上 CDN 版。
  **先决策**：MediaPipe 全本地化 25MB 是否值得入库？
    - 方案A（离线优先，抗墙）：改 importmap 指本地 three、`hands.locateFile` 指 `vendor/mediapipe/`，核查 0 字节的 `hands_solution_simd_wasm_bin.data` 是否有效，入库 25MB。
    - 方案B（轻量）：删 vendor，仅本地化 three.js(1.3MB)，MediaPipe 保 CDN + 加载超时检测 + 中文失败提示 + unpkg 备用兜底。
  验收：断网/CDN 故障时不白屏、有中文提示 + 重试；摄像头被拒有友好提示。

- [ ] **Haven 首屏拆包** — `games/haven/`
  断点：agent 仅分析了 base64（favicon/brand=icon.jpg、title-bg=keyart.jpg，3 份重复 icon 约 912KB），无改动。
  做：`index.html` 入口改外链 `haven.js`+`styles.css`+`assets/`，别再内联 1.6MB 单文件；若有 sync 脚本要理解其意图别搞坏。
  验收：首屏更快出画面、存档兼容、世界生成/建造/UI 逐项正常、console 零报错。**若判断拆包收益低/风险高，如实写"不建议拆"并说明。**

---

## P1 · 头牌玩法（用户核心诉求：上强度）

- [ ] **坦克"更像坦克" 步骤 2–6** — `games/tank-strike/index.html`（步骤1已上线：车身/炮塔分离+履带印）
  - [ ] 步2 开炮重量感：后坐（车身后仰+炮塔回弹已埋 `recoilV/turretRecoil/hullPitch` 字段，待接演出）、装填进度环、炮口烟圈
  - [ ] 步3 装甲角度：正/侧/背装甲，命中角决定跳弹（火花+叮声）/穿透；敌方同理鼓励绕后
  - [ ] 步4 弹种切换：AP(高穿低爆)/HE(溅射打集群)/破甲，数字键切（player 已有 `ammo:'AP'` 字段）
  - [ ] 步5 地形掩体：沙丘/残骸可挡弹可摧毁；软沙降速硬地提速
  - [ ] 步6 敌人军种：轻型快车绕后/重型需绕侧/自行火炮远程抛射/步兵反坦克组/多部位可破 Boss
  验收：每步浏览器点测手感；`__game` 覆盖新系统。

- [ ] **沙海奇境 多群系 + 探索目标** — `games/minecraft/index.html`
  断点：agent 加过 6 种群系方块定义(SALT/SALT_CRYSTAL/BLACKSAND/BASALT/BANDED/GLOW_ORE，atlas 209–214)但未接生成逻辑，已回退。空闲图集格：209–232、235–255。
  - [ ] 先修开始页"文字墙"→简洁开始卡 + 游戏内 H 帮助面板/首玩分步引导
  - [ ] 多群系世界：枯骨盐湖/风蚀峡谷/地下晶洞/古代遗迹带/黑沙火山，各有专属材质色调+生物+资源
  - [ ] 探索目标：散布遗迹/沉船/商队残骸(图纸/稀材/剧情)+可标记地图或指南针
  - [ ] 资源链分级 + 营地→要塞 + 环境事件(沙暴/流沙/温差/海市蜃楼) + 生态昼夜分层 + 载具机动
  验收：换区域画面与玩法都变；深度优先于数量。

---

## P2 · 全站打磨（各游戏共性短板，可穿插做）

- [ ] **音频**（三款自承短板）：设置面板音乐/音效独立音量+静音；音效多变体避免听腻；关键节点听感区分；BGM 随战况。全 WebAudio 合成不下载。
- [ ] **首因体验**：主菜单动态背景+标题演出+音乐淡入；载入过渡不硬切。
- [ ] **可读性/新手引导**：分步引导可跳过；图标状态可悬停查解释。
- [ ] **设置完备**：画质档位(低配关粒子/阴影)、键位说明、完整暂停菜单。
- [ ] **深渊圣所**（本地 Desktop 版更超前，仓库版待同步）：新敌人待机/前摇/死亡逐帧动画（art.js 自承没做到）；铁砧/石台/熔炉美术细节收尾。
- [ ] **首页 P2**：设备需求标签（竹知了=摄像头、深渊=键鼠）；封面统一风格。

---

## 已知环境事实

- 部署：GitHub Pages（主 `bosswenwu.github.io/alex-games/`）+ Vercel 镜像（`alex-games-wheat.vercel.app`），推 `main` 两处都更新。
- 曾有并发 agent 带 git 权限直接提交推送——多方协作时注意先 `git pull`/核对 origin 再推，避免互相覆盖。
- 反复撞「每周额度」硬顶。续接靠各目录 `PROGRESS.md` 断点。
